<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:oooh"
             x:Class="oooh.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <SearchBar x:Name="BarraPesquisa" 
                       Placeholder="Pesquisa" 
                       SearchCommand="{Binding PesquisaCommand}" 
                       Text="{Binding Valor, Mode=TwoWay}">
            </SearchBar>
            <Label Text="Você Pesquisou:" />
            <Label Text="{Binding Valor}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
